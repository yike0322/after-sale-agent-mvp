spring:
  datasource:
    url: jdbc:h2:mem:after_sale_agent;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: true

management:
  health:
    redis:
      enabled: false

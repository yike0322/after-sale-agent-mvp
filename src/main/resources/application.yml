spring:
  application:
    name: after-sale-agent
  profiles:
    default: mock
    group:
      test: mock
  ai:
    vectorstore:
      type: none

management:
  endpoints:
    web:
      exposure:
        include: health
